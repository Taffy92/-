# 万能格式转换器正式发布全量审查报告

审查时间：2026-05-28  
审查范围：Web 静态站点、离线安装版下载入口、发布文档、SHA256 校验文件、CloudBase 下载函数、CloudBase 静态托管正式发布状态。

## 结论

当前版本已经完成正式发布到 CloudBase 默认域名，线上回读验证通过。发布页、发布说明、安装指南、SHA256 校验文件均可访问，中文内容未发现乱码。安装包未被静态托管直接公开，下载入口继续通过 `createDownloadUrl` 云函数控制。

本轮审查未发现新的阻断级 BUG。仍需关注的事项为正式运营层面的收尾：真实域名备案和绑定、Windows 代码签名、发布前 Git 提交范围审查、以及静态托管对不存在安装包路径的 SPA 回退优化。

## 已执行检查

1. 单元与业务测试
   - 执行 `vitest run`
   - 结果：11 个测试文件通过，51 个测试通过

2. 下载函数本地安全测试
   - 执行 `scripts/test-create-download-url.cjs`
   - 结果：通过
   - 覆盖点：请求体限制、Content-Type 校验、密码校验、文件类型限制、CORS 响应

3. 隐私与网络边界测试
   - 执行隐私与网络守卫测试
   - 结果：2 个测试文件通过，14 个测试通过
   - 结论：第三方上传行为仍被阻断，符合本地处理原则

4. 静态构建
   - 执行 Next.js 静态构建
   - 结果：构建成功，生成 15 个静态页面

5. 浏览器可用性测试
   - 执行静态站点 Playwright 测试
   - 结果：6 个端到端测试通过
   - 覆盖点：首页/下载页、工具区响应式布局、PDF/Word/Excel 本地预览、隐私网络限制

6. 发布文件编码检查
   - 检查发布页、发布说明、安装指南、SHA256 文件
   - 结果：未发现 Unicode 替换字符，线上中文内容正常

7. 安装包静态公开性检查
   - 检查静态托管目录 `release/v1.0.0/installers/`
   - 结果：仅存在目录占位与 `SHA256SUMS.txt`
   - 结论：EXE/MSI 未通过静态站点直接公开

## 已执行部署

1. 更新 CloudBase 云函数
   - 函数：`createDownloadUrl`
   - 状态：更新成功
   - 作用：继续作为离线安装包受控下载入口

2. 上传 CloudBase 静态托管
   - 源目录：`apps/web/out`
   - 目标路径：`/`
   - 状态：上传成功

3. 线上回读验证
   - 首页：HTTP 200
   - 下载页：HTTP 200，包含“下载离线安装版”“发布说明”“安装指南”
   - 发布说明：HTTP 200，中文正常
   - 安装指南：HTTP 200，中文正常
   - SHA256 校验文件：HTTP 200，包含当前 EXE 哈希
   - 下载函数健康检查：HTTP 200

## 当前线上地址

- Web 默认域名：`https://format-converter-prod-x-d71bce41-1434109188.tcloudbaseapp.com/`
- 下载页：`https://format-converter-prod-x-d71bce41-1434109188.tcloudbaseapp.com/download/`
- 下载函数：`https://format-converter-prod-x-d71bce41.service.tcloudbase.com/createDownloadUrl`

## 仍需关注事项

1. 真实域名尚未绑定
   - 原因：备案仍需等待管局审核
   - 建议：备案完成后再绑定正式域名、配置 HTTPS 证书，并重新执行线上回读验证

2. Windows 安装包仍需代码签名
   - 影响：未签名安装包可能触发 Windows Defender 或 SmartScreen 提示
   - 建议：正式对外大规模分发前完成 EV/OV 代码签名，并更新发布说明

3. Git 提交范围需要单独审查
   - 当前工作区存在历史修改、报告文件、截图和 `.pnpm-store/`
   - 建议：提交前只选择本次需要纳入版本的源码、配置和文档，避免提交临时截图、缓存目录和过程报告

4. 静态托管 SPA 回退可优化
   - 当前不存在的安装包静态路径不会返回安装包二进制，但会回退到 HTML 页面
   - 风险等级：低
   - 建议：后续为 `/release/v1.0.0/installers/*.exe` 和 `*.msi` 配置更明确的 404 或禁用回退策略，减少误判

## 发布判定

本轮审查、修复链路和正式部署已完成。当前版本可作为 CloudBase 默认域名下的正式发布版本使用。上线后建议优先处理真实域名、代码签名和 Git 提交范围审查。
