# CLEAN_VM_TEST_SAMPLE_FILES_README

用途：说明人工干净 Windows 10 / Windows 11 虚拟机测试时需要准备哪些样例文件。

请只使用测试文件，不要使用真实隐私文件、合同、证件、财务资料或客户文件。

## 1. 推荐目录结构

在虚拟机中创建：

```text
D:\万能格式转换器测试\
  输入 文件夹\
  输出 文件夹\
  截图\
  日志\

C:\Users\当前用户\Downloads\万能格式转换器测试\
C:\Users\当前用户\Desktop\测试 文件夹\
```

如果没有 D 盘，请先给虚拟机添加一个 D 盘。无法添加时，在测试结果模板中记录“D 盘路径测试未执行：无 D 盘”。

## 2. 必备样例文件清单

请准备以下文件，并放入：

`D:\万能格式转换器测试\输入 文件夹`

| 文件名 | 用途 | 要求 |
| --- | --- | --- |
| `中文音频.wav` | 测试 WAV 转 FLAC、中文文件名 | 3 到 10 秒即可 |
| `带 空格 音频.wav` | 测试带空格文件名 | 3 到 10 秒即可 |
| `中文视频.mp4` | 测试 MP4 转 WebM、中文文件名 | 3 到 10 秒即可 |
| `带 空格 视频.mp4` | 测试带空格文件名 | 3 到 10 秒即可 |
| `损坏音频.wav` | 测试失败提示和队列继续 | 故意损坏，不能正常播放 |
| `损坏视频.mp4` | 测试失败提示和队列继续 | 故意损坏，不能正常播放 |
| `不支持格式.txt` | 测试不支持文件跳过或提示 | 普通文本文件 |

## 3. 如何准备正常 WAV 文件

可选方式：

1. 使用 Windows 自带“录音机”录一段 3 到 10 秒音频，再另存或转换为 WAV。
2. 使用你已有的无隐私测试音频，转换为 WAV 后改名。
3. 使用开源或自制测试音频，不要使用有版权争议或隐私内容的文件。

文件名必须至少包含：

1. `中文音频.wav`
2. `带 空格 音频.wav`

## 4. 如何准备正常 MP4 文件

可选方式：

1. 使用手机或录屏工具录制一段 3 到 10 秒的普通测试视频。
2. 内容建议是空白桌面、测试画面或无隐私素材。
3. 文件体积不需要大，建议 1MB 到 20MB。

文件名必须至少包含：

1. `中文视频.mp4`
2. `带 空格 视频.mp4`

## 5. 如何准备损坏音频文件

在 PowerShell 中执行：

```powershell
Set-Content -LiteralPath "D:\万能格式转换器测试\输入 文件夹\损坏音频.wav" -Value "this is not a valid wav file" -Encoding ASCII
```

这个文件扩展名是 `.wav`，但内容不是有效 WAV，用于验证失败提示和队列继续。

## 6. 如何准备损坏视频文件

在 PowerShell 中执行：

```powershell
Set-Content -LiteralPath "D:\万能格式转换器测试\输入 文件夹\损坏视频.mp4" -Value "this is not a valid mp4 file" -Encoding ASCII
```

这个文件扩展名是 `.mp4`，但内容不是有效 MP4，用于验证失败提示和队列继续。

## 7. 如何准备不支持格式

在 PowerShell 中执行：

```powershell
Set-Content -LiteralPath "D:\万能格式转换器测试\输入 文件夹\不支持格式.txt" -Value "unsupported file for import test" -Encoding UTF8
```

用于验证：

1. 文件夹导入时是否跳过不支持文件。
2. 软件是否给出合理提示。
3. 不支持文件不会导致整个队列失败。

## 8. 推荐复制一份到 C 盘用户目录

为了测试 C 盘用户目录权限，也建议复制一份到：

```text
C:\Users\当前用户\Downloads\万能格式转换器测试
C:\Users\当前用户\Desktop\测试 文件夹
```

测试目的：

1. 验证 `$DOWNLOAD/**` 路径可用。
2. 验证 `$DESKTOP/**` 路径可用。
3. 验证 C 盘用户目录可以正常导入和输出。

## 9. 样例文件质量要求

正常样例：

1. WAV 能用系统播放器播放。
2. MP4 能用系统播放器播放。
3. 文件名保留中文和空格。
4. 不要太大，避免把测试焦点变成性能压力。

损坏样例：

1. 扩展名保留 `.wav` 或 `.mp4`。
2. 内容故意不是有效媒体。
3. 预期转换失败。
4. 失败后队列必须继续处理后续正常文件。

## 10. sidecar 实验测试组合

建议至少执行以下组合：

| 输入文件 | 输出格式 | 预期后端 | 预期结果 |
| --- | --- | --- | --- |
| `中文音频.wav` | FLAC | sidecar | 成功 |
| `带 空格 音频.wav` | FLAC | sidecar | 成功 |
| `中文视频.mp4` | WebM | sidecar | 成功 |
| `带 空格 视频.mp4` | WebM | sidecar | 成功 |
| `中文音频.wav` | MP3 | WASM | 不走 sidecar |
| `中文音频.wav` | AAC | WASM | 不走 sidecar |
| `中文音频.wav` | M4A | WASM | 不走 sidecar |
| `中文视频.mp4` | MP4/H.264 | WASM | 不走 sidecar |
| `损坏音频.wav` | FLAC | sidecar 尝试后失败 | 失败提示清楚，队列继续 |
| `损坏视频.mp4` | WebM | sidecar 尝试后失败 | 失败提示清楚，队列继续 |
| `不支持格式.txt` | 任意 | 不应进入有效转换 | 跳过或提示不支持 |

## 11. 截图建议

建议截图：

1. SHA256 校验结果。
2. WebView2 Runtime 检查结果。
3. 断网状态。
4. 安装界面。
5. 首次启动界面。
6. sidecar 实验开关默认关闭。
7. sidecar 开启后状态。
8. WAV 转 FLAC 成功。
9. MP4 转 WebM 成功。
10. 损坏文件失败提示。
11. 任务历史脱敏显示。
12. Defender / SmartScreen 提示，如有。

截图保存目录建议：

`D:\万能格式转换器测试\截图`

## 12. 不要做的事

1. 不要用真实敏感文件测试。
2. 不要把测试文件上传到任何网站。
3. 不要在在线版测试 sidecar，sidecar 只属于离线专业版内部实验。
4. 不要把 MP3、AAC/M4A、MP4/H.264 结果标记为 sidecar 正式能力。
5. 不要在联网状态下混淆判断外部请求。
