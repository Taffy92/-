# SIDECAR_FFMPEG_TEST_PLAN

生成时间：2026-05-24

本测试计划用于未来 sidecar FFmpeg 实施后的验证。本轮只生成计划，不执行。

## 1. 测试目标

验证 Windows 离线专业版 sidecar FFmpeg：

1. 能断网运行。
2. 能处理常见音视频格式。
3. 能接入批量任务队列。
4. 能处理中文路径、D 盘路径、带空格路径。
5. 不上传用户文件。
6. 不执行任意命令。
7. 不破坏在线版 FFmpeg WASM。

## 2. 安装包资源测试

| 测试项 | 预期 |
| --- | --- |
| `ffmpeg.exe` 是否存在 | 安装目录或资源目录中存在 |
| `ffprobe.exe` 是否存在 | 存在，或文档说明未打包原因 |
| SHA256 是否匹配 | 与 `SHA256SUMS.txt` 一致 |
| 许可证文件是否存在 | `LICENSES/` 存在 |
| 构建参数是否存在 | `BUILD_CONFIG.txt` 存在 |
| 源码获取说明是否存在 | `SOURCE_OFFER.txt` 存在 |

## 3. sidecar 可执行测试

1. 启动程序。
2. 检测 sidecar。
3. 执行 `ffmpeg -version` 等价内部检查。
4. 执行 `ffprobe` 媒体信息读取。
5. 无网络环境下重复执行。

预期：

1. 不需要联网。
2. 不弹出命令行窗口。
3. 不暴露原始命令给用户。
4. 失败时显示中文友好提示。

## 4. 单文件转换测试

| 测试项 | 输入 | 输出 | 预期 |
| --- | --- | --- | --- |
| MP4 转 MP4 压缩 | `.mp4` | `.mp4` | 成功或按 LGPL 构建能力给出明确限制 |
| MP4 转 WebM | `.mp4` | `.webm` | 成功 |
| MOV 转 MP4 | `.mov` | `.mp4` | 成功或给出编码限制 |
| WAV 转 MP3 | `.wav` | `.mp3` | 成功或按许可证策略灰度 |
| MP3 转 WAV | `.mp3` | `.wav` | 成功 |
| 视频提取音频 | `.mp4` | `.mp3/.wav/.m4a/.aac` | 成功或按输出编码策略限制 |

## 5. 路径兼容测试

测试路径：

1. `D:\万能格式转换器测试\输入 文件夹\视频 样例.mp4`
2. `D:\万能格式转换器测试\输出 文件夹`
3. `C:\Users\当前用户\Downloads\万能格式转换器测试`
4. `C:\Users\当前用户\Desktop\测试 文件夹`
5. 长文件名目录。
6. 带空格目录。

测试项：

1. 中文文件名。
2. 带空格路径。
3. D 盘路径。
4. 长文件名。
5. 输出目录不存在。
6. 无权限目录。

预期：

1. 合法路径成功。
2. 非法或无权限路径只失败当前任务。
3. 错误提示清楚。
4. 日志脱敏。

## 6. 损坏文件测试

| 测试项 | 预期 |
| --- | --- |
| 损坏视频 | 当前任务失败，显示“文件可能已损坏或编码不受支持” |
| 损坏音频 | 当前任务失败，队列继续 |
| 无音轨视频提取音频 | 显示“该视频没有可提取的音频轨道” |
| 0 字节文件 | 当前任务失败 |

## 7. 大文件和取消测试

测试项：

1. 1 分钟视频。
2. 大码率视频。
3. 转换中取消。
4. 取消后输出临时文件清理。
5. 取消后队列状态为“已取消”。
6. 取消后可重试。

预期：

1. 大文件不会卡死 UI。
2. 取消只影响当前任务。
3. 队列继续或按用户取消策略停止。
4. 不产生损坏结果文件，或明确标记为未完成。

## 8. 批量队列测试

测试项：

1. 多个视频批量转换。
2. 多个音频批量转换。
3. 视频批量提取音频。
4. 一个失败后继续处理下一项。
5. 失败原因展示。
6. 失败任务重试。
7. 清空已完成任务。
8. 清空全部任务。

预期：

1. 每个任务独立状态。
2. 进度更新稳定。
3. 失败不影响其他任务。
4. 任务历史保存最近 100 条。

## 9. 输出和结果管理测试

测试项：

1. 输出目录写入。
2. 输出文件重名处理。
3. 打开结果文件。
4. 打开输出目录。
5. 复制输出路径。
6. 导出处理日志。

预期：

1. 输出路径为本地路径。
2. 结果文件存在。
3. 日志中路径脱敏。
4. 不上传日志。

## 10. 隐私网络测试

断网环境确认：

1. 图片处理不联网。
2. 文档处理不联网。
3. 音频处理不联网。
4. 视频处理不联网。
5. 任务历史不上传。
6. 处理日志不上传。
7. 输出目录路径不上传。
8. sidecar FFmpeg 不联网。
9. CloudBase 不接触用户文件。
10. 广告不接收用户文件。
11. 外部请求为 0。

## 11. 安全测试

文件名包含：

1. 空格。
2. 中文。
3. `&`
4. `;`
5. `|`
6. `'`
7. `"`
8. 括号。

预期：

1. 文件名只作为路径参数。
2. 不被 shell 执行。
3. 不影响命令结构。
4. 不产生任意命令执行。

## 12. 在线版回归测试

必须确认：

1. 在线版仍包含 `/ffmpeg/ffmpeg-core.js`。
2. 在线版仍包含 `/ffmpeg/ffmpeg-core.wasm`。
3. 在线音频转换可用。
4. 在线视频转换可用。
5. 在线视频提取音频可用。
6. 在线版不进入批量任务队列。
7. 在线版无外部 FFmpeg 请求。

## 13. 断网安装测试

使用最终 EXE/MSI：

1. Windows 10 x64，断网，无 WebView2 Runtime。
2. Windows 11 x64，断网，无 WebView2 Runtime。
3. 有 WebView2 Runtime。
4. 安装、启动、卸载、重装。

预期：

1. WebView2 offlineInstaller 生效。
2. sidecar FFmpeg 文件存在。
3. 离线专业版可启动。
4. 音视频批量处理断网可用。

## 14. 通过标准

满足以下条件才可进入正式发布：

1. 所有 P0 测试通过。
2. 没有外部上传请求。
3. sidecar SHA256 可验证。
4. 许可证文件齐全。
5. 干净 Windows 10/11 VM 通过。
6. GPL/LGPL 复核完成。
7. 安装包可卸载、可重装。
