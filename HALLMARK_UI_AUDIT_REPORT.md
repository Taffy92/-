# Hallmark UI Audit Report

审查时间：2026-05-23

审查范围：

- 在线网页版：`/tools`
- 离线专业版：`NEXT_PUBLIC_APP_MODE=desktop` 首页工作台
- 视口：375px、768px、1440px

本轮只做 UI 审查，没有修改项目代码。

## 1. 使用的审查规则

本轮按以下 Skill 口径审查：

- `hallmark`：反 AI 模板感、结构同质化、廉价 SaaS 模板痕迹、空洞营销文案、结构节奏；
- `frontend-design`：界面层级、响应式、按钮和表单可用性、产品一致性；
- `web-design-guidelines`：工具站清晰度、移动端可用性、视觉克制、内容可信度；
- `screenshot`：基于真实截图做 375px、768px、1440px 检查。

Hallmark 自评：

```text
Philosophy 4 / Hierarchy 4 / Execution 4 / Specificity 4 / Restraint 4 / Variety 3
```

结论：当前 UI 已经明显好过普通 AI 生成模板，但仍有一些“工具站通用感”和“桌面专业版细节占位感”。不建议大改结构，适合进入小范围打磨阶段。

## 2. 截图依据

本轮刷新或查看的截图：

- `verification/ui-round-2-tools-mobile-375.png`
- `verification/ui-round-2-tools-tablet-768.png`
- `verification/ui-round-2-tools-desktop-1440.png`
- `verification/hallmark-desktop-workbench-1440.png`
- `verification/hallmark-desktop-workbench-768.png`

Playwright UI 检查结果：

- 在线版 375px、768px、1440px 通过；
- 下载页授权说明通过；
- 未发现页面级横向溢出；
- `#ad-container` 数量正确；
- 当前功能 `aria-pressed` 状态正确。

非 UI 问题：

- 截图里的黑色 `N` 浮层来自本地 Next.js 开发环境，不属于生产 UI。
- 本地 dev server 仍有 `D:\System Volume Information` Watchpack 扫描提示，不影响 UI 审查。

## 3. 总体判断

当前视觉方向是正确的：

- 在线版已经从“营销站首页”转向“轻量工具入口”；
- 离线专业版已经从“网页套壳”转向“工作台软件”；
- 隐私表达比常见工具站更可信；
- 功能卡片、文件添加、参数区、结果区之间的流程基本清楚；
- 没有明显过度渐变、发光、巨大 hero、虚假数据和廉价证明条。

主要不足：

- 在线版仍有一点“卡片式 SaaS 模板”痕迹；
- 离线专业版虽然结构像工作台，但任务队列、右侧参数和状态栏还偏静态占位；
- 桌面专业版在 768px 宽度下变成顶部横向导航，专业软件的“左侧导航稳定感”减弱；
- 卡片圆角和留白略偏大，商业工具感可以再收紧；
- 部分文案仍偏说明型，少数位置可以更像真实软件提示。

## 4. 在线网页版 Audit

### 4.1 是否还有 AI 模板感

结论：有轻微残留，但不严重。

表现：

- 大白卡片 + 浅蓝背景 + 大圆角 + 图标功能卡，是常见 AI 工具站/低代码 SaaS 模板组合；
- 首屏文案结构仍接近“标签 + 大标题 + 一段解释 + 提示框”；
- 功能卡片的视觉语言较统一，但每个功能的内容差异较小，略有模板复用感。

建议微调：

- 将首屏大卡片的圆角从当前偏大的风格收紧一些；
- 隐私提示框可拆成更像产品内通知条，而不是完整信息卡；
- 功能卡片中可加入更贴近功能的短状态词，例如“适合头像/证件照”“默认导出 JPG”“逐页或合成长图”，减少统一模板感。

不建议：

- 不建议换成大面积插画、3D 图或强渐变；
- 不建议增加复杂首页动画；
- 不建议再做营销式 hero。

### 4.2 是否像廉价 SaaS 模板

结论：不像廉价模板，但还有“通用工具站组件库”痕迹。

优点：

- 没有虚假客户 Logo、虚假数据、夸张增长指标；
- 功能直接进入处理流程；
- “文件不上传服务器”的承诺放在核心位置，可信度较好。

风险点：

- 主容器和所有卡片都使用较接近的圆角、阴影和浅青色选中态，页面层次略单一；
- 桌面端功能卡片排布规整但略平，缺少一两个更强的信息锚点。

建议微调：

- 常用功能卡片和非图片类功能卡片可以拉开层级，例如常用功能更紧凑、文档/音视频更偏列表式；
- 参数区可以更像“设置面板”，减少白卡片套白卡片的感觉。

### 4.3 渐变、大圆角、营销文案

结论：控制得比较克制。

- 渐变：没有明显过度使用；
- 大圆角：略偏多，尤其在线版大容器和功能卡；
- 营销文案：没有明显空洞吹嘘，但首屏说明文字略长。

建议：

- 在线版首屏说明可以再短一点，把支持格式放到功能卡或工具区；
- “浏览器本地处理”标签保留，这是可信锚点。

### 4.4 在线版是否轻量、可信、清楚

结论：基本达标。

优点：

- 用户一眼能看到“万能格式转换器”；
- 文件添加区位置正确，不漂浮、不遮挡；
- 功能选择在文件添加之前，流程更自然；
- 隐私说明明确提到不上传服务器；
- PDF 功能边界说明清楚，避免误解为 PDF 编辑器。

主要问题：

- 移动端 375px 下功能卡横向滑动，虽然没有页面级横向溢出，但用户可能不知道还能横向滑动；
- 移动端首屏信息偏长，用户要向下滚动才能开始操作；
- 下载结果按钮在未处理前禁用是正确的，但视觉上可以更明确“处理后可下载”。

建议微调：

- 375px 下功能区增加轻微的“横向可滑动”视觉暗示；
- 移动端首屏隐私说明压缩为两三行，详细边界放到折叠说明；
- 禁用下载按钮旁边增加短提示：“完成后自动启用”。

## 5. 离线专业版 Audit

### 5.1 是否真的像商业级桌面软件

结论：已经具备商业级桌面软件骨架，但还没有完全达到成熟商业工具质感。

达标部分：

- 1440px 下左侧导航、中间工作区、右侧参数面板、底部状态栏结构清楚；
- 顶部指标卡能表达任务队列状态；
- 任务表格、进度条、结果管理、参数面板均已具备；
- “离线可用”“不调用云端转换 API”表达清楚；
- 整体不像网站首页套壳。

不足部分：

- 任务表格为空时仍偏静态演示感；
- 右侧参数面板里有一些设置中心类占位项，和当前“图片裁切”任务关联不够强；
- 底部状态栏有专业软件感，但信息密度还可以更像真实任务运行状态；
- 顶部导航仍保留网站式横向导航，桌面软件感略被削弱。

建议微调：

- 右侧参数面板根据功能变化显示更具体的专业参数，减少通用占位；
- 任务队列空状态可以更像真实软件：提供“添加文件”“选择批量功能”“导入文件夹”三个动作；
- 底部状态栏增加更真实的运行信息字段，例如“空闲 / 正在处理 / 已暂停”；
- 顶部导航在离线版中可以弱化，把主要导航权重留给左侧栏。

不建议：

- 不建议再做大面积渐变或科技风背景；
- 不建议加入首页式宣传区；
- 不建议用巨大的图标卡替代任务队列。

### 5.2 左侧导航

结论：1440px 下专业，768px 下专业感下降。

优点：

- 左侧导航命名清楚；
- 首页工作台选中态明显；
- 分组顺序合理：工作台、工具、批量、结果、教程、政策、设置、关于。

问题：

- 768px 截图中左侧导航变成顶部横向导航，横向排布较像网页导航；
- 选中态样式和在线版功能按钮较接近，缺少桌面软件的“导航轨道”感。

建议：

- 如果离线软件窗口最小宽度允许，建议保持左侧导航，不要在 768px 直接变横向；
- 也可以在窄窗口下使用可折叠左栏，只显示图标和短标题。

### 5.3 中间任务队列

结论：结构正确，但需要更真实的任务状态细节。

优点：

- 文件添加区、任务表格、进度条、结果管理顺序合理；
- 空状态文案清楚；
- 开始处理、取消、清空任务的操作顺序合理。

问题：

- 任务表格列较少，商业批处理工具通常还会有大小、输出格式、耗时、失败原因；
- “开始处理”在没有任务时仍然可见，虽然可以接受，但更专业的做法是禁用或提示先添加文件；
- 空状态不够“可操作”，只是提示暂无任务。

建议：

- 无任务时禁用“开始处理”，或点击后显示明确提示；
- 表格列下一轮可扩展为：文件名、类型、大小、输出格式、状态、耗时、操作；
- 空状态加入“添加文件夹 / 添加文件 / 选择输出目录”的动作入口。

### 5.4 右侧参数面板

结论：方向正确，具体性不足。

优点：

- 参数面板固定在右侧，符合桌面专业工具习惯；
- 当前功能标题和参数表单可见；
- 本地处理承诺放在右侧底部，位置合理。

问题：

- 当前功能是“图片裁切”，但面板里同时出现输出目录、命名规则、覆盖策略、EXIF、并发数量等概念时，用户可能以为这些都已完整可用；
- 右侧信息层级可以再紧凑一些，减少大白块。

建议：

- 当前功能参数和高级批量参数分成两个折叠组；
- 对尚未实装的专业设置标记“专业版批量任务中生效”；
- 每个参数区标题更短，例如“裁切设置”“导出设置”“批量设置”。

### 5.5 底部状态栏

结论：有商业软件感，值得保留。

优点：

- “离线可用 / 版本 / 任务 / 成功 / 失败 / 输出目录”很像真实桌面工具；
- 比网页 footer 更适合离线专业版。

建议微调：

- 增加状态词：空闲、处理中、已暂停、处理完成；
- 输出目录如果尚未真实选择，应显示“默认下载目录”，避免用户误以为已经能选择系统目录；
- 可将大文件提示放到状态栏右侧，减少中间区域文案重复。

## 6. 隐私表达 Audit

结论：可信，且比大多数在线转换站更清楚。

可信点：

- 明确写出“文件仅在本地处理，不上传服务器”；
- 说明 CloudBase 只用于下载授权，不接触用户处理文件；
- 说明广告脚本不接收处理文件；
- PDF 功能边界说明明确。

可微调点：

- 在线版首屏隐私框内容略长，可保留一句强承诺，详细说明放到次级位置；
- 离线版右侧承诺区可以加入“断网可处理”的短句，让离线版差异更明显。

不建议：

- 不建议用“100% 安全”“绝对隐私”等过度承诺；
- 不建议删除广告脚本说明，广告合规需要保留。

## 7. 按钮、卡片、表格、进度条、错误提示统一性

### 按钮

结论：主次关系基本清楚。

- 主按钮青蓝色，醒目；
- 取消任务红色，语义明确；
- 下载结果禁用态可识别。

建议：

- 无任务时开始处理按钮可禁用；
- 禁用下载结果按钮可以加说明，减少用户困惑；
- 在线版和离线版主按钮高度可以统一。

### 卡片

结论：统一但略模板化。

建议：

- 卡片圆角稍收紧；
- 功能卡和内容卡视觉层级拉开；
- 离线版指标卡可以更像仪表盘，不必和在线版卡片完全同一语言。

### 表格

结论：离线版表格具备雏形。

建议：

- 表头和空状态还可以更紧凑；
- 增加失败原因列会显著提升专业感；
- 成功/失败状态建议使用统一 badge。

### 进度条

结论：清楚但偏基础。

建议：

- 批量任务时显示当前文件名；
- 增加百分比和数量组合，例如 `3 / 20 · 15%`；
- 失败时进度条旁显示“查看失败原因”。

### 错误提示

结论：本轮截图没有触发错误态，无法做完整视觉判断。

建议：

- 保持错误提示为“发生了什么 + 下一步怎么做”；
- 不要只显示技术堆栈；
- 批量任务失败要能定位到具体文件。

## 8. 响应式 Audit

### 375px 在线版

结论：可用，但首屏偏长。

问题：

- 首屏标题和说明占用较多空间；
- 功能卡横向滚动需要更明确的暗示；
- 开始处理区域在较深位置，用户需要滚动。

建议：

- 缩短首屏说明；
- 隐私承诺压缩；
- 功能卡横向区域增加渐隐边缘或“左右滑动查看更多”。

### 768px

在线版：

- 平板布局可用；
- 没有明显横向溢出。

离线版：

- 能使用，但更像平板网页；
- 专业桌面软件感弱于 1440px；
- 如果目标是 Windows 桌面专业版，建议设置合理最小窗口宽度，或保留折叠侧栏。

### 1440px

在线版：

- 清楚、稳定、可信；
- 留白较大但不夸张；
- 工具流程完整。

离线版：

- 专业工作台结构清楚；
- 左中右三区成立；
- 底部状态栏有效；
- 仍需用真实任务队列状态增强商业成熟度。

## 9. 分级问题清单

### P1：建议下一轮优先微调

1. 离线版 768px 下左侧导航消失，专业软件感下降。
2. 离线版任务队列空状态偏静态，应增加真实动作入口。
3. 无任务时“开始处理”建议禁用或给出明确提示。
4. 右侧参数面板存在通用占位感，应按当前任务拆分“当前参数 / 批量参数”。

### P2：可逐步优化

1. 在线版首屏文案略长。
2. 在线版卡片圆角和白卡片风格略模板化。
3. 移动端功能横向滑动缺少提示。
4. 进度条缺少当前文件和数量表达。
5. 表格缺少大小、输出格式、耗时、失败原因等专业字段。

### P3：不急于处理

1. 色彩风格整体可保留；
2. 主按钮颜色可保留；
3. 顶部品牌和图标当前够清楚；
4. 隐私表达已经可信，不需要大改；
5. 在线版整体流程不建议再重构。

## 10. 哪些地方只需要微调

- 卡片圆角和阴影；
- 首屏文案长度；
- 移动端横向滑动提示；
- 禁用按钮提示；
- 空任务状态；
- 表格字段；
- 状态 badge；
- 右侧参数面板分组；
- 底部状态栏文案。

这些都属于局部 UI 质量提升，不需要重写页面。

## 11. 哪些地方不建议再改

不建议继续改：

- 在线版“单页工具流”结构；
- 在线版“功能选择 → 添加文件 → 参数设置 → 开始处理 → 下载结果”的顺序；
- 离线版左中右工作台基本结构；
- 底部状态栏；
- 本地处理和隐私承诺的位置；
- `#ad-container` 底部广告位置；
- 顶部品牌名称和图标。

这些已经符合当前产品定位，再大改会增加风险。

## 12. 最终建议

当前 UI 可以进入发布前细节打磨，不建议再做第三轮大规模视觉重构。

建议下一轮只做“小刀口”：

1. 离线专业版：任务队列空状态、按钮禁用态、表格字段、右侧参数分组；
2. 在线版：移动端首屏压缩、功能横向滑动提示、下载按钮禁用说明；
3. 两端统一：状态 badge、错误提示、进度条语义；
4. 保留现有整体结构，不要换主题，不要上重动画，不要加营销区。

发布判断：

- 在线版：可以作为公开 MVP 发布；
- 离线专业版：界面骨架可发布，但如果定位“专业版收费/授权”，建议至少补强批量任务的真实状态和失败原因展示后再作为正式商业版主卖点。

