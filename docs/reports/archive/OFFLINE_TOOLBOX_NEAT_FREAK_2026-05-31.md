# 离线工具箱 neat-freak 记录

日期：2026-05-31

## 范围

本轮同步离线专业版工具箱改造后的项目说明、下载页文案和发布说明，不重新打包、不部署。

## 已同步

1. 离线版说明改为专业工具箱定位：顶部命令栏、左侧分类工具、中央上传/任务队列、右侧参数面板。
2. 批量能力说明改为跟随当前转换工具，不再作为离线版单独功能页描述。
3. 批量覆盖范围同步为：图片尺寸调整、图片加水印、图片压缩、PDF 转图片、Word 转图片、Excel 转图片、视频转换、音频转换和视频提取音频。
4. 在线教程删除“两个版本共用同一套主要界面”的旧说法。
5. 保留本地处理、无登录、无云端转换、离线版不渲染在线广告容器等边界。

## 验证状态

本轮为文档和文案同步。进入本轮前，离线工具箱界面已经通过桌面构建、隐私/批量测试和 Playwright 截图检查；本轮完成后已重新执行 `npm run build:web`，确保 `apps/web/out` 是在线版产物。

## 未处理

1. 未重新生成 EXE/MSI 安装包。
2. 未部署在线站点。
3. 未做断网 Windows 10/11 干净机安装回归。
