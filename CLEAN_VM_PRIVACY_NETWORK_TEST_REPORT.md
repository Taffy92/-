# CLEAN_VM_PRIVACY_NETWORK_TEST_REPORT

本报告用于干净 Windows VM 的隐私与网络隔离验收。当前会话没有可操作的断网干净 VM，因此不伪造测试通过结果；以下为当前配置结论和待 VM 实测项。

## 当前配置结论

| 检查项 | 当前配置状态 |
| --- | --- |
| 图片处理不上传 | 已有 networkGuard 和静态冒烟验证 |
| 文档处理不上传 | 静态冒烟验证无外部请求 |
| 音频处理不上传 | 静态冒烟验证无外部请求 |
| 视频处理不上传 | 静态冒烟验证无外部请求 |
| 任务历史不上传 | 历史保存在本机 localStorage，路径脱敏 |
| 处理日志不上传 | 日志本地生成 |
| 输出目录路径不上传 | 当前逻辑仅本地展示/打开 |
| CloudBase 不接触用户文件 | CloudBase 仅用于下载授权 |
| 广告不接收用户文件 | 广告组件与 File/Blob/Canvas 处理隔离 |
| 图片压缩 worker | 本地 `/vendor/browser-image-compression/browser-image-compression.js` |
| FFmpeg wasm | 本地静态资源 |
| WebView2 安装 | `offlineInstaller`，安装包内含离线安装器 |

## 干净 VM 必测项

| 项目 | 结果 |
| --- | --- |
| 断网安装 EXE | 待 VM 实测 |
| 断网安装 MSI | 待 VM 实测 |
| 断网启动程序 | 待 VM 实测 |
| 断网图片批量压缩 | 待 VM 实测 |
| 断网 Word 批量转图片 | 待 VM 实测 |
| 断网 Excel 批量转图片 | 待 VM 实测 |
| 断网音频批量转换 | 待 VM 实测 |
| 断网视频批量转换 | 待 VM 实测 |
| 断网视频提取音频 | 待 VM 实测 |
| 断网任务历史保存 | 待 VM 实测 |
| 断网导出日志 | 待 VM 实测 |
| 断网打开输出目录 | 待 VM 实测 |
| 断网打开单个结果文件 | 待 VM 实测 |

## 网络监控建议

在 VM 中建议同时使用以下方式：

1. 关闭网络适配器，确认安装和运行不依赖联网。
2. 开启网络但用防火墙阻断程序外联，观察是否有错误。
3. 使用 Windows 资源监视器查看进程网络活动。
4. 使用 Fiddler Classic / Proxyman / Wireshark 记录是否存在外部请求。
5. 对 `万能格式转换器.exe` 单独观察，确认处理文件时没有上传连接。

## 判定标准

通过标准：

1. 断网状态下能安装、启动和完成核心功能。
2. 处理用户文件时没有任何用户文件、路径、日志、任务历史、转换结果上传。
3. 图片压缩 worker 和 FFmpeg wasm 都从本地资源加载。
4. WebView2 Runtime 安装不需要联网。

失败标准：

1. 断网无 WebView2 时安装失败或启动失败。
2. 处理图片、文档、音频、视频时出现外部上传请求。
3. 程序要求联网下载 worker、wasm、WebView2 或转换组件。
4. 日志或历史记录保存完整敏感路径并可被上传。

## 当前结论

当前配置符合本地处理设计，但干净 VM 网络隔离验收尚未执行。正式发布前必须在断网 VM 中复测，特别是 WebView2 离线安装、FFmpeg 本地 wasm 和图片压缩本地 worker。
