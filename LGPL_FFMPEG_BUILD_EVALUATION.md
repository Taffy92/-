# LGPL_FFMPEG_BUILD_EVALUATION

生成时间：2026-05-24

本文件评估 Windows 离线专业版未来使用 LGPL FFmpeg 构建的可行性。本文件不是法律意见，正式商业发布前需要做许可证和法律复核。

## 1. 什么是 LGPL FFmpeg 构建

FFmpeg 官方项目通常以 LGPL 为基础授权，但如果构建时启用了 GPL 组件或 nonfree 组件，最终二进制的许可证状态会变化。

LGPL FFmpeg 构建的核心目标：

1. 不启用 GPL 组件。
2. 不启用 nonfree 组件。
3. 保留 LGPL 许可证文本和源码获取方式。
4. 记录完整构建参数。
5. 让商业发布的许可证风险低于当前 `@ffmpeg/core` GPL WASM。

## 2. 哪些构建选项可能导致 GPL

以下选项或库通常会导致 GPL 风险，需要避免或单独复核：

1. `--enable-gpl`
2. `libx264`
3. `libx265`
4. `libxvid`
5. `libvidstab`
6. 其他 FFmpeg 文档中标记为 GPL 的外部库

本项目当前 WASM 代码中使用了 `libx264`，这也是当前 GPL 风险重点之一。

## 3. 哪些构建选项可能导致 nonfree

以下选项会引入 nonfree 风险或更复杂的再分发限制，需要避免或单独复核：

1. `--enable-nonfree`
2. 某些专有 SDK 编码器。
3. 需要专有授权的硬件编码 SDK。
4. 不允许自由再分发的第三方库。

## 4. 明确不能启用的选项

长期 LGPL 构建方案中不能启用：

```text
--enable-gpl
--enable-nonfree
```

如果必须启用其中任何一个，不能再按 LGPL 构建对外宣传，应重新进入 GPL 或 nonfree 合规专项。

## 5. 当前功能对 LGPL 构建的满足度

| 功能 | LGPL 构建可行性 | 风险点 | 建议 |
| --- | --- | --- | --- |
| MP4 转换 | 可行但需谨慎 | H.264 编码器选择 | 默认可支持 MP4 容器，H.264 编码需单独评估 |
| WebM 转换 | 较可行 | VP9/Opus 构建选项 | 可作为默认支持 |
| MOV 转换 | 可行 | 常见输出仍可能走 H.264/AAC | 默认转 MP4，编码器需确认 |
| MP3 转换 | 需要复核 | `libmp3lame` 许可证和专利历史 | 可灰度支持，商业发布前复核 |
| WAV 转换 | 可行 | PCM 基本风险低 | 默认支持 |
| AAC / M4A 转换 | 需要复核 | AAC 编码器、专利和平台差异 | 可灰度支持，商业发布前复核 |
| FLAC 转换 | 可行 | 风险较低 | 默认支持 |
| 视频提取音频 | 可行 | 取决于输出编码格式 | WAV/FLAC 默认，MP3/AAC 复核 |

## 6. 如果不用 libx264，H.264 编码如何处理

如果目标是 LGPL 构建，不能直接使用 `libx264`，除非接受 GPL。

可选方案：

1. 使用 FFmpeg 原生可用编码器，但需要确认质量、兼容性和许可证状态。
2. MP4 输出优先做 remux 或低风险编码组合，适用于源视频编码已兼容的场景。
3. 默认推荐 WebM/VP9 或其他 LGPL 友好路径。
4. 将 H.264 输出标记为“高兼容模式”，只有在 GPL 构建或合法授权构建中开放。
5. 后续提供两个 FFmpeg 包：
   - LGPL 标准包。
   - GPL 扩展包，并完整履行 GPL 义务。

需要注意：用户通常期望 MP4/H.264，因此商业产品体验和许可证策略之间需要取舍。

## 7. MP3 / AAC 许可证和专利风险

MP3 和 AAC 涉及历史专利、编码器许可证、地区差异等问题。

技术上可能可用，但商业发布前应确认：

1. 使用的编码器许可证。
2. 构建参数。
3. 目标销售地区。
4. 是否需要额外专利许可。
5. 是否存在应用商店或分发平台要求。

本文件不替代法律意见。

## 8. 推荐默认支持格式

在 LGPL 优先策略下，建议默认支持：

视频：

1. WebM 输出。
2. MP4 容器输出，但 H.264 编码能力需根据实际 LGPL 构建确认。
3. MOV 输入转 MP4 或 WebM。

音频：

1. WAV。
2. FLAC。
3. M4A/AAC 作为常用格式，但发布前复核。
4. MP3 作为常用格式，但发布前复核。

视频提取音频：

1. WAV。
2. FLAC。
3. M4A/AAC 和 MP3 视复核结果开放。

## 9. 建议实验标记的格式

建议灰度或实验标记：

1. MP4/H.264 输出。
2. MOV 输出。
3. AVI 输出。
4. MKV 输出。
5. MP3 输出。
6. AAC/M4A 输出。

原因：

1. 用户期望较高。
2. 兼容性差异大。
3. 编码器许可证和专利问题需要复核。

## 10. 商业发布前必须人工确认事项

商业发布前必须确认：

1. FFmpeg 构建来源是否可信。
2. 构建命令是否完整保存。
3. 是否启用了 `--enable-gpl`。
4. 是否启用了 `--enable-nonfree`。
5. 是否包含 `libx264`、`libx265`、`libmp3lame` 等外部库。
6. 每个外部库许可证。
7. 是否需要提供源码。
8. 是否需要提供对象文件或重链接方式。
9. 是否需要在安装目录中保留许可证。
10. 是否需要在官网和软件内展示许可证。
11. 目标销售地区是否涉及专利许可问题。
12. 法律顾问或许可证专家复核。

## 11. 结论

LGPL FFmpeg 构建适合作为离线专业版长期商业路线，但不能只凭“FFmpeg 是 LGPL”就直接发布。

建议：

1. 先做 sidecar 技术验证。
2. 同时验证 LGPL 构建是否满足核心格式。
3. 如果 LGPL 构建无法满足用户对 MP4/H.264/MP3 的需求，再评估 GPL 构建和完整 GPL 合规。
4. 不建议在没有许可证复核前移除当前 GPL 说明。
