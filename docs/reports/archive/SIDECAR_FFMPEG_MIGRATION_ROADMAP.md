# SIDECAR_FFMPEG_MIGRATION_ROADMAP

生成时间：2026-05-24

本路线图定义离线专业版从 FFmpeg WASM 迁移到 sidecar FFmpeg 的分阶段方案。

## 阶段 1：sidecar 可行性验证，不替换现有功能

修改范围：

1. 新增最小 Rust command。
2. 打包测试用 `ffmpeg.exe`。
3. 新增开发内部测试脚本。
4. 不接入正式 UI。

风险：

1. Tauri sidecar 路径解析失败。
2. NSIS/MSI 打包资源遗漏。
3. Windows Defender 误报。

回滚方案：

1. 删除测试资源。
2. 不启用 command。
3. 保持现有 WASM。

测试项：

1. `ffmpeg.exe` 存在。
2. SHA256 匹配。
3. MP4 转 WebM。
4. WAV 转 MP3。
5. 中文路径。
6. D 盘路径。

是否需要重新打包：是。

是否影响在线版：否。

是否影响离线专业版：不应影响现有功能。

## 阶段 2：离线专业版增加 sidecar 实验开关

修改范围：

1. 新增桌面设置项或内部配置。
2. sidecar 只在实验开关开启时使用。
3. 失败时自动回退 WASM。

风险：

1. 用户误以为实验功能完全稳定。
2. 输出格式兼容性不一致。

回滚方案：

1. 关闭实验开关。
2. 默认继续 WASM。

测试项：

1. 单文件视频转换。
2. 单文件音频转换。
3. 视频提取音频。
4. 失败回退。
5. 不上传文件。

是否需要重新打包：是。

是否影响在线版：否。

是否影响离线专业版：只影响开启实验开关的用户。

## 阶段 3：离线专业版音视频默认使用 sidecar

修改范围：

1. desktop 模式音视频默认选择 sidecar。
2. 批量队列音视频默认选择 sidecar。
3. 输出结果直接写入输出目录。
4. 前端只显示路径和状态。

风险：

1. LGPL 构建格式能力不如当前 WASM。
2. 部分格式转换失败率变化。
3. 安装包体积增加。

回滚方案：

1. 配置切回 WASM。
2. 保留 WASM 静态资源。

测试项：

1. 音频批量转换。
2. 视频批量转换。
3. 视频批量提取音频。
4. 失败任务重试。
5. 取消任务。
6. 任务历史。

是否需要重新打包：是。

是否影响在线版：否。

是否影响离线专业版：是，需要完整回归。

## 阶段 4：保留 WASM 作为回退

修改范围：

1. sidecar 不可用时使用 WASM。
2. sidecar 单任务失败时不自动重跑 WASM，避免耗时翻倍；只提示用户可手动切换回退。
3. 设置中心提供“兼容模式”。

风险：

1. 双后端行为不一致。
2. 许可证声明需要同时覆盖 WASM 和 sidecar。

回滚方案：

1. 保留当前 WASM 默认。
2. 关闭 sidecar。

测试项：

1. sidecar 文件缺失。
2. sidecar SHA256 不匹配。
3. WASM 回退可用。

是否需要重新打包：是。

是否影响在线版：否。

是否影响离线专业版：可控。

## 阶段 5：完成许可证和 Notices 更新

修改范围：

1. 更新 `THIRD_PARTY_NOTICES.md`。
2. 更新 `FFMPEG_LICENSE_NOTICE.md`。
3. 更新 `OPEN_SOURCE_LICENSES.md`。
4. 更新安装目录 Notices。
5. 更新网站和软件许可证页面。

风险：

1. 构建来源不清晰。
2. 构建参数遗漏。
3. LGPL/GPL 状态判断错误。

回滚方案：

1. 不发布 sidecar 版本。
2. 保持当前 GPL WASM 声明。

测试项：

1. Notices 包含 FFmpeg。
2. 安装目录离线可查看。
3. 源码获取方式明确。

是否需要重新打包：是。

是否影响在线版：可能影响许可证页面内容，不影响功能。

是否影响离线专业版：不影响功能。

## 阶段 6：干净 Windows 10/11 VM 验证

修改范围：无功能改动。

测试项：

1. Windows 10 x64 断网安装。
2. Windows 11 x64 断网安装。
3. 无 WebView2 Runtime。
4. sidecar 存在。
5. 批量音视频处理。
6. D 盘路径。
7. 中文路径。
8. 带空格路径。
9. 外部请求为 0。

风险：

1. 打包资源遗漏。
2. WebView2 离线安装失败。
3. sidecar 被杀毒误报。

回滚方案：

1. 发布不含 sidecar 的当前版本。
2. sidecar 作为后续版本灰度。

是否需要重新打包：否，使用候选包测试。

是否影响在线版：否。

是否影响离线专业版：验证发布候选。

## 阶段 7：正式发布

发布前条件：

1. 干净 VM 测试通过。
2. FFmpeg 构建来源确认。
3. 许可证复核完成。
4. Notices 完整。
5. EXE/MSI SHA256 已记录。
6. 安装、卸载、重装通过。
7. 断网全功能通过。

风险：

1. 许可证义务未完全履行。
2. 用户环境差异。
3. 大文件性能问题。

回滚方案：

1. 保留上一版安装包。
2. 发布说明标注回滚版本。
3. 关闭 sidecar 默认开关。

是否需要重新打包：是，生成正式发布包。

是否影响在线版：否。

是否影响离线专业版：是，正式引入 sidecar。

## 路线图结论

不建议立即一次性替换当前 FFmpeg WASM。推荐先做 sidecar 可行性验证，再以实验开关进入离线专业版，最后在干净 Windows VM 和许可证复核完成后改为默认。
