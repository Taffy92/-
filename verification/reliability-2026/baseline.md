# 转换可靠性改进前基线

- 记录日期：2026-08-08
- 代码版本：`1df30d13`
- 用途：仅作为改进前基线，不代表正式发布验收通过

## 标准环境

- 操作系统：Windows 10 专业版 64 位，版本 10.0.19045
- 处理器：Intel Core i7-9700
- 内存：15.9 GiB
- Node.js 20.20.2
- pnpm 9.15.4
- Rust 1.95.0
- Cargo 1.95.0
- LibreOffice 26.2.5，Windows x86-64
- FFmpeg n7.1.4-6-g181cfa1008-20260523
- FFprobe n7.1.4-6-g181cfa1008-20260523

Node.js 20 使用 Node 官方 Windows x64 便携包，并在下载后核对官方 SHA256。便携运行时位于 Git 已忽略的临时目录，不进入发布产物。

## 自动化测试基线

在加入本任务的样本矩阵测试之前，使用 Node.js 20 执行根目录 `npm test`：

- 27 个测试文件通过；
- 143 项测试通过；
- 1 个测试文件包含跳过项；
- 2 项测试被跳过；
- 跳过项均位于 `officeImages.test.ts`，分别是 DOCX 与 Excel 的真实渲染验证。

加入样本矩阵测试但尚未创建清单时，新增的 2 项测试按预期失败；其余结果保持不变。该红灯状态只用于证明测试能检测缺失基线。

## 当前结构风险

- `ToolsClient.tsx` 同时承担文件输入、任务调度、后端选择、转换、结果写入和 UI 状态。
- `LocalToolsClient.tsx` 同时承担本地工具执行、取消、输出写入和 UI 状态。
- `globals.css` 集中包含在线与离线产品壳的大量规则。
- 多项现有测试通过读取源码字符串守住边界，但不能代替真实文件行为验证。
- Office 真实渲染是当前唯一明确跳过的关键转换路径。

## 发布与商业风险

- 系统默认 Node.js 不是正式要求版本，必须通过 `npm run verify:toolchain` 阻断错误环境。
- Windows 安装包当前没有 Authenticode 签名，SmartScreen 风险仍存在。
- 授权后台的跨域保护改动尚在用户工作区，未纳入本基线提交。
- 授权后台登录边缘限流尚未完成生产配置与黑盒验收。

## 本阶段未执行

- 未运行 EdgeOne 正式构建；
- 未重新打包 MSI；
- 未执行 Windows 11 干净虚拟机验收；
- 未对真实转换样本测量耗时或内存峰值；
- 未对现有转换实现作任何修复。

这些项目将在后续任务中逐项建立失败基线并验证。
