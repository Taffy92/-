# FFmpeg sidecar bin placeholder

当前没有放入 `ffmpeg.exe` 或 `ffprobe.exe`。

不要从不明来源下载二进制文件放入此目录。

正式放入前必须确认：

- 来源可信；
- 允许再分发；
- 构建参数完整；
- 未启用 `--enable-gpl`，除非决定按 GPL 发布；
- 未启用 `--enable-nonfree`；
- 已记录 SHA256；
- 已保留许可证文本和源码获取方式。
