{
  "name": "desktop",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "node ../../scripts/prepare-libreoffice.mjs && tauri build --bundles msi",
    "build:all": "tauri build --bundles msi",
    "package": "pnpm build",
    "package:msi": "node ../../scripts/prepare-libreoffice.mjs && tauri build --bundles msi"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.6.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.0"
  }
}
