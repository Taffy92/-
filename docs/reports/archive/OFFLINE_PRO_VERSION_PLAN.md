# 离线专业版规划

审查日期：2026-05-22  
目标：让离线版成为更专业、更具体、更像商业级工具的 Windows 本地软件，并保证无网络安装和全功能使用。

## 1. 定位

在线版：轻量单文件工具，适合快速处理。  
离线专业版：批量、本地、断网、稳定、可审计，适合大量文件和敏感文件。

## 2. 离线安装要求

必须做到：

- 安装包不依赖联网下载运行库。
- 内置 WebView2 离线安装方案或明确打包离线 WebView2 runtime。
- 内置 JS/CSS、PDF.js worker、FFmpeg WASM、图标、字体、静态资源。
- 核心功能不依赖服务器、账号或云 API。
- 授权下载只影响“下载安装包”，不影响用户安装后的本地功能。

当前已看到 Tauri 配置中设置了 WebView offline installer 模式，但由于配置乱码，正式发布前必须重新打包并在断网虚拟机中安装验证。

## 3. 专业版专属功能

只放在离线版，不放在线版：

- 图片批量压缩。
- 图片批量加水印。
- Word 批量转图片。
- Excel 批量转图片。
- 视频批量格式转换。
- 音频批量格式转换。
- 视频批量提取音频。
- 输出目录选择。
- 批量命名规则。
- 失败重试和任务日志。

## 4. 推荐界面

离线版首页直接进入“工作台”：

- 左侧：功能导航。
- 中间：批量文件队列。
- 右侧：参数和输出设置。
- 底部：状态栏、版本号、离线状态、任务统计。

不要把离线版做成网站首页。用户安装桌面软件后，预期是打开就能处理文件。

## 5. 本地引擎建议

图片：

- 浏览器 Canvas + Worker 足够覆盖大部分场景。
- 批量任务要限制并发，避免内存暴涨。

PDF：

- PDF.js 渲染继续可用。
- 批量 PDF 转图片需要页数限制和失败项隔离。

Word/Excel：

- 当前 Canvas 重绘适合轻量模式。
- 专业版建议增加本地渲染引擎，例如 LibreOffice headless 或其他可离线分发的转换器。
- 引擎必须内置或随安装包分发，不能安装后联网下载。

音视频：

- FFmpeg WASM 可用，但大文件性能有限。
- 专业版可以考虑 Tauri sidecar 调用本地 FFmpeg 可执行文件，提升速度、稳定性和格式兼容。
- 必须处理 FFmpeg 许可证合规。

## 6. 发布检查

每次发版必须检查：

- 在无网络 Windows 10 x64 上安装。
- 在无网络 Windows 11 x64 上安装。
- 启动后不访问外网也能打开所有页面。
- 图片、PDF、Word、Excel、视频、音频核心样例能处理。
- 处理过程中没有上传用户文件。
- 安装包名称、发布者、版权、图标显示正确。
- 安装包 SHA256 与下载页一致。
- Notices 和开源许可证页面完整。

