# CLEAN_VM_OFFLINE_FUNCTION_TEST_REPORT

本报告用于干净 Windows VM 中验证离线专业版功能。当前会话没有可操作的干净 VM，因此不伪造功能通过结果；本报告记录测试准备要求、现有样例文件、待实测项目和预期判定标准。

## VM 内建议目录

请在干净 VM 内准备：

1. `D:\万能格式转换器测试\输入 文件夹`
2. `D:\万能格式转换器测试\输出 文件夹`
3. `C:\Users\当前用户\Downloads\万能格式转换器测试`
4. `C:\Users\当前用户\Desktop\测试 文件夹`

## 样例文件要求

| 类型 | 要求 | 当前项目已有样例 |
| --- | --- | --- |
| JPG 图片 | 正常图片、中文名、带空格名 | `图片 一.jpg` |
| PNG 图片 | 正常图片、中文名、带空格名 | `图片 二.png` |
| 损坏图片 | 用于失败原因和重试测试 | `损坏 图片.jpg` |
| Word 文档 | `.docx` | `标准 文档.docx`、`测试 文档.docx` |
| Excel 表格 | `.xlsx` | `标准 表格.xlsx`、`测试 表格.xlsx` |
| MP4 视频 | 用于视频批量转换 | `测试 视频.mp4` |
| WAV/MP3 音频 | 用于音频批量转换 | `测试 音频.wav` |
| 不支持格式 | 验证自动跳过 | `跳过 文本.txt` |

当前项目样例目录：

`D:\万能格式转换器项目\verification\offline-smoke\输入 文件夹`

## 功能测试矩阵

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| D 盘文件夹导入 | 待 VM 实测 | 需要真实 Tauri dialog + fs.scope 验证 |
| C 盘用户目录导入 | 待 VM 实测 | 应在 Downloads/Desktop 下测试 |
| C 盘非用户目录导入 | 待 VM 实测 | 当前已移除 `C:/**`，预期可能受限 |
| 自动跳过不支持文件 | 待 VM 实测 | txt 应跳过并显示跳过数量 |
| 图片批量压缩 | 待 VM 实测 | 目标 JPG，断网可用 |
| Word 批量转图片 | 待 VM 实测 | 逐页/合成导出按 UI 当前能力验证 |
| Excel 批量转图片 | 待 VM 实测 | 多 Sheet 应可处理或给出明确提示 |
| 音频批量转换 | 待 VM 实测 | FFmpeg 本地 wasm |
| 视频批量转换 | 待 VM 实测 | FFmpeg 本地 wasm |
| 失败任务显示原因 | 待 VM 实测 | 损坏图片应失败且有明确原因 |
| 失败任务重试 | 待 VM 实测 | 重试按钮可用 |
| 取消任务 | 待 VM 实测 | 长任务或队列中任务取消 |
| 清空已完成任务 | 待 VM 实测 | 成功任务可清空 |
| 任务历史保存 | 待 VM 实测 | 关闭重开后最近历史保留 |
| 打开结果文件 | 待 VM 实测 | 中文路径、带空格路径都要测 |
| 打开输出目录 | 待 VM 实测 | `shell.open` 原生行为 |
| 导出处理日志 | 待 VM 实测 | 日志不应含完整敏感路径 |

## 当前已有非 VM 自动化证据

上一轮静态冒烟已通过：

1. 图片批量压缩：通过。
2. Word 批量转图片：通过。
3. Excel 批量转图片：通过。
4. 音频批量转换：通过。
5. 视频批量转换：通过。
6. 外部请求：0。

限制：这些证据来自本机 Playwright + 静态产物，不等同于安装后的 Tauri 原生权限层、系统对话框、真实输出目录写入和 `shell.open` 验收。

## 当前结论

离线功能干净 VM 验收尚未执行。正式发布前必须在干净 Windows 10/11 VM 中完成至少一轮 EXE 和 MSI 的真实安装、断网运行、D 盘路径、C 盘用户目录、中文路径和带空格路径测试。
