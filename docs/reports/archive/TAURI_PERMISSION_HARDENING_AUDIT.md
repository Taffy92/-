# TAURI_PERMISSION_HARDENING_AUDIT

本轮目标是审查并尽可能收窄离线专业版 Tauri 权限，重点是文件系统、路径、对话框和打开本地结果文件能力。审查范围限定在离线专业版发布风险，不重做 UI，不修改在线版主流程，不改变 WebView2 离线安装策略，不新增云端转换。

## 当前权限用途

| 权限 | 当前状态 | 实际用途 | 审查结论 |
| --- | --- | --- | --- |
| `dialog.open` | 启用 | 选择输出目录、导入文件夹 | 必须保留 |
| `dialog.save` | 启用 | 后续导出日志或保存结果时可用 | 可保留 |
| `dialog.message` / `dialog.confirm` / `dialog.ask` | 启用 | 桌面端提示、确认类交互 | 可保留 |
| `dialog.all` | 启用 | 覆盖所有 dialog 子权限 | 可以收窄为 `false`，改用显式子权限 |
| `fs.readFile` | 启用 | 从用户选择的文件夹读取图片、Word、Excel、音视频文件 | 必须保留 |
| `fs.readDir` | 启用 | 文件夹导入、扫描支持格式 | 必须保留 |
| `fs.writeFile` | 启用 | 写入批量处理结果、导出处理日志 | 必须保留 |
| `fs.exists` | 启用 | 打开单个结果文件前检查文件是否存在 | 必须保留 |
| `fs.createDir` | 启用 | 未来创建默认输出目录或日志目录 | 可保留 |
| `fs.copyFile` | 启用 | 当前代码未直接使用 | 可以收窄为 `false` |
| `fs.removeFile` / `fs.removeDir` / `fs.renameFile` | 关闭 | 当前不需要删除、重命名用户文件 | 必须保持关闭 |
| `fs.all` | 启用 | 覆盖所有文件系统能力 | 可以收窄为 `false`，改用显式子权限 |
| `fs.scope` | 用户目录 + `C:/**` + `D:/**` | 支持用户目录、桌面、下载、图片、视频、音频、D 盘项目目录 | 可尝试移除 `C:/**`，暂保留 `D:/**` |
| `path.all` | 启用 | 获取下载目录、处理 Windows 路径、输出路径拼接 | 本轮暂不收窄，避免破坏路径兼容 |
| `shell.open` | 启用 | 打开输出目录、打开单个结果文件 | 必须保留 |
| `process` | 关闭 | 不需要退出/重启进程能力 | 必须保持关闭 |
| `updater` | 未启用 | 当前不做自动更新 | 必须保持关闭 |
| WebView2 `offlineInstaller` | 启用 | 完全断网安装时安装本地 WebView2 Runtime | 必须保持不变 |

## 必须保留的权限

必须保留 `dialog.open`、`fs.readFile`、`fs.readDir`、`fs.writeFile`、`fs.exists`、`path.all`、`shell.open`。这些权限分别支撑文件夹导入、输出目录选择、批量任务写入结果、打开输出目录、打开单个结果文件和 Windows 路径兼容。

## 可以尝试收窄的权限

1. `dialog.all` 可以改为 `false`，保留必要子权限。
2. `fs.all` 可以改为 `false`，保留必要子权限。
3. `fs.copyFile` 当前未发现业务调用，可以改为 `false`。
4. `fs.scope` 中的 `C:/**` 可以先移除。用户目录、桌面、文档、下载、图片、视频、音频已经覆盖大多数 C 盘用户文件场景。

## 暂不建议修改的权限

1. `D:/**`：当前项目长期使用 D 盘路径，用户明确要求支持 D 盘路径、中文路径和带空格路径。严格移除会明显提高破坏风险。
2. `path.all`：当前 Tauri v1 前端 API 使用 `path.downloadDir()`，同时批量输出路径拼接依赖 Windows 路径兼容。本轮不做 API 级拆分。
3. `shell.open`：成功任务需要打开结果文件和输出目录。收窄前必须在真实 Windows Tauri 窗口中验证。

## 需要真实 Windows 测试后才能决定的事项

1. 移除 `C:/**` 后，用户选择 `C:\非用户目录\...` 作为输入或输出目录是否会失败。
2. Tauri v1 在用户通过系统对话框选择目录后，是否能动态授权该目录下读写。
3. `shell.open` 对本地文件路径、目录路径、中文路径和带空格路径的兼容性。
4. MSI / NSIS 安装后的真实 WebView2、权限和路径行为。

## 如果保留 `C:/**` 和 `D:/**` 的发布说明建议

如果正式发布前仍保留宽 scope，应在隐私政策或离线版说明中明确：

1. 软件只读取用户主动添加的文件或文件夹。
2. 软件只向用户选择的输出目录或默认下载目录写入结果。
3. 软件不会自动扫描系统磁盘。
4. 软件不会上传图片、文档、音频、视频、任务历史或处理日志。
5. 批量处理日志和历史记录会脱敏显示路径，不保存文件内容。

## 本轮建议

推荐执行“半收窄方案”：关闭 `dialog.all` 和 `fs.all`，移除 `C:/**`，保留用户目录和 `D:/**`，并保持 `path.all`、`shell.open`、WebView2 `offlineInstaller` 不变。该方案能降低发布权限风险，同时尽量不破坏离线专业版核心批量能力。
