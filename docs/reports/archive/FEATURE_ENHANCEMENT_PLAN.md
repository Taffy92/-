# 功能增强计划

审查日期：2026-05-22  
目标：加强图片、视频、音频、Word、Excel 的本地处理能力，同时保证用户文件不上传服务器。

## 1. 图片处理

当前基础能力已经覆盖裁切、尺寸调整、添加水印、图片压缩。下一阶段增强：

- 在线版保留单文件处理，避免变复杂。
- 离线专业版增加批量压缩、批量加水印、批量尺寸调整。
- 增加输出目录、文件命名规则、覆盖策略、失败重试。
- 压缩结果统一优先 JPG，可根据源文件透明通道提示用户改用 PNG/WebP。
- 增加质量预览：原图大小、结果大小、压缩率、肉眼质量提示。
- 增加 EXIF/元数据清理选项，默认清理隐私位置数据。

## 2. PDF 转图片

当前 PDF 转图片适合继续使用 PDF.js 本地渲染。增强建议：

- 支持逐页导出 ZIP。
- 支持合成一张长图。
- 增加页码范围校验和超大 PDF 分批提示。
- 对 PDF.js worker 加载失败、字体异常、加密 PDF、损坏 PDF 做更明确提示。
- 离线专业版可以增加批量 PDF 转图片。

## 3. Word 转图片

当前实现通过读取 `.docx` XML 并用 Canvas 绘制，只适合基础段落和简单表格。增强路线：

- 在线版保留轻量转换，并明确“复杂排版可能不完全一致”。
- 离线专业版增加更高保真本地渲染方案：
  - 方案 A：内置或引导使用本地 LibreOffice headless 导出 PDF，再用 PDF.js/本地图像渲染转图片。
  - 方案 B：使用 Tauri sidecar 调用本地转换引擎，所有处理仍在用户电脑完成。
  - 方案 C：保留浏览器 Canvas 方案作为快速模式。
- 支持批量 Word 转图片应只放在离线专业版。

## 4. Excel 转图片

当前实现用 SheetJS 读取单元格并 Canvas 绘制，适合简单表格。增强路线：

- 在线版保留轻量转换。
- 离线专业版增加更高保真本地渲染：
  - 优先转换为 PDF 或 HTML，再渲染为图片。
  - 保留工作表分页、合并单元格、列宽、行高、字体、边框。
  - 批量 Excel 转图片只放在离线专业版。
- 对复杂表格增加“请人工核对”的明确提示。

## 5. 视频格式转换

当前 `packages/media-core` 使用 FFmpeg WASM 支持 MP4、MOV、AVI、MKV、WebM。增强建议：

- 在线版：限制文件大小和时长，推荐小文件快速转换。
- 离线版：增加批量队列、并发数限制、失败重试和输出目录。
- 增加预设：原始尺寸、1080p、720p、480p；高质量、推荐、小体积。
- 增加编码说明：MP4/MOV 使用 H.264 + AAC；WebM 使用 VP9 + Opus；AVI 使用 MPEG-4 + MP3。
- 对浏览器内存不足、编码器不可用、无音轨等错误做中文友好提示。

## 6. 音频格式转换与视频提取音频

当前支持 MP3、WAV、AAC、M4A、FLAC，提取音频支持 MP3、WAV、M4A、AAC。增强建议：

- 增加码率选择：96k、128k、192k、256k。
- 增加采样率说明，默认 44.1kHz。
- 离线版支持批量音频转换、批量视频提取音频。
- 增加“保留/清理元数据”开关。
- 对无音轨视频、损坏音频、编码失败提供明确处理建议。

## 7. 本地处理边界

所有增强必须遵守：

- 不上传原始文件。
- 不上传转换结果。
- 不上传文件内容、OCR 文本或媒体内容。
- 不调用云端转换 API。
- 广告、授权下载接口与文件处理模块隔离。

