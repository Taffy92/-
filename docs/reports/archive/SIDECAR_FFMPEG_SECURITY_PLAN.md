# SIDECAR_FFMPEG_SECURITY_PLAN

生成时间：2026-05-24

本文件定义离线专业版 sidecar FFmpeg 的安全边界。本轮不修改权限和代码。

## 1. 总原则

sidecar FFmpeg 必须只用于本地媒体转换。

不得：

1. 执行用户输入的任意命令。
2. 上传用户文件。
3. 上传日志。
4. 上传路径。
5. 上传转换结果。
6. 调用云转换 API。
7. 扩大 Tauri 网络权限。
8. 开启 Tauri `process.all`。

## 2. 禁止用户输入任意命令

前端不得提供“自定义 FFmpeg 参数”输入框。

Rust command 不接受：

1. 原始命令字符串。
2. 原始 args 数组。
3. 用户提供的 `ffmpeg.exe` 路径。
4. 用户提供的额外 filter 参数。

前端只能提交结构化参数。

## 3. 只允许白名单参数

允许字段：

1. `mode`
2. `inputPath`
3. `outputDir`
4. `outputName`
5. `outputFormat`
6. `quality`
7. `videoSize`
8. `audioBitrate`
9. `stripMetadata`

所有字段都必须白名单校验。

输出格式白名单：

1. 视频：`mp4`、`mov`、`avi`、`mkv`、`webm`
2. 音频：`mp3`、`wav`、`aac`、`m4a`、`flac`
3. 提取音频：`mp3`、`wav`、`m4a`、`aac`

## 4. 输入文件必须来自用户主动选择

输入文件来源只能是：

1. 用户通过文件选择器添加。
2. 用户通过文件夹导入添加。

不得允许：

1. 前端传任意系统路径让 Rust 读取。
2. 自动扫描系统目录。
3. 递归扫描根目录。

建议 Rust 侧验证：

1. 路径存在。
2. 是文件。
3. 扩展名在支持列表内。
4. 路径在 Tauri 允许范围内。

## 5. 输出文件必须写入用户选择输出目录

输出目录来源：

1. 用户选择的输出目录。
2. 默认下载目录。

不得写入：

1. Windows 系统目录。
2. 程序安装目录。
3. 任意未授权路径。
4. 输入文件原目录，除非用户明确选择该目录为输出目录。

## 6. 不允许覆盖系统文件

输出文件名必须由程序生成。

建议规则：

1. 清理非法字符。
2. 限制长度。
3. 默认不覆盖已有文件。
4. 已存在时追加序号：`_001`、`_002`。
5. 不允许 `..` 路径穿越。
6. 不允许绝对路径作为文件名。

## 7. 不允许执行除 ffmpeg.exe / ffprobe.exe 之外的程序

Rust 侧只允许调用打包内置路径：

1. `ffmpeg.exe`
2. `ffprobe.exe`

不得调用：

1. 系统 PATH 中的 ffmpeg。
2. 用户选择的 exe。
3. `cmd.exe`
4. `powershell.exe`
5. 其他任意程序。

## 8. 不允许 shell 拼接不可信字符串

必须使用参数数组。

正确方式：

```rust
Command::new(ffmpeg_path).args(args)
```

禁止：

```rust
Command::new("cmd").args(["/c", user_command])
```

## 9. 中文路径和空格路径

必须使用：

1. `PathBuf`
2. 参数数组
3. 不手动加引号
4. 不拼接 shell 字符串

这样可以自然支持：

1. 中文路径。
2. D 盘路径。
3. 带空格路径。
4. 长文件名。

## 10. 失败日志必须脱敏

对用户展示和导出的日志只保留：

1. 文件名。
2. 脱敏路径。
3. 输出格式。
4. 友好失败原因。
5. 耗时。

脱敏示例：

```text
D:/.../demo.mp4
C:/.../result.mp3
```

不得记录完整身份证件、合同、财务文件路径。

## 11. 不记录完整敏感路径

任务历史继续使用：

`sanitizeLocalPath`

sidecar 后端返回给前端的完整路径只用于：

1. 打开结果文件。
2. 打开输出目录。
3. 写入结果。

导出日志和任务历史中只保存脱敏路径。

## 12. 不上传日志、文件、结果

sidecar 功能不得新增任何上传行为。

网络允许范围仍然维持现有产品策略：

1. 官网静态资源。
2. 广告脚本。
3. CloudBase 下载授权。
4. 软件更新检查。
5. 用户主动打开官网或帮助文档。

用户处理文件不得发送给这些服务。

## 13. 不新增网络权限

Tauri 配置不应因为 sidecar 增加网络权限。

sidecar FFmpeg 不需要网络。

建议在调用参数中增加：

```text
-nostdin
```

并避免任何网络输入协议。

可考虑禁用网络协议支持的 FFmpeg 构建，降低误用风险。

## 14. 不打开 Tauri process 全量权限

不应开启：

```json
"process": { "all": true }
```

前端不应直接获得进程执行能力。

进程执行应仅在 Rust command 内部实现，且只执行内置 FFmpeg。

## 15. 最小权限配置

如果必须新增权限，建议只新增 sidecar 所需配置：

1. `bundle.externalBin`
2. `bundle.resources`

前端 allowlist 不需要开放任意命令执行。

当前 `shell.open` 仅用于打开输出目录和结果文件，应继续保留但不扩大。

## 16. 安全测试

必须测试：

1. 用户不能传入自定义命令。
2. 文件名包含空格时转换正常。
3. 文件名包含中文时转换正常。
4. 文件名包含 `&`、`;`、`|` 等字符时不会被当成命令执行。
5. 输出目录不存在时失败。
6. 输出目录无权限时失败。
7. 损坏文件不会中断整个批量队列。
8. 取消任务只取消当前进程。
9. 日志不包含完整路径。
10. 断网运行外部请求为 0。

## 17. 安全结论

sidecar FFmpeg 可以比 WASM 更适合离线专业版大文件和批量处理，但安全边界必须放在 Rust 侧。

核心要求：

1. 结构化请求。
2. 白名单参数。
3. 内置 sidecar 路径。
4. 参数数组。
5. 本地输出目录。
6. 日志脱敏。
7. 不新增网络和 process 全量权限。
